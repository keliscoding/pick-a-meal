import { SliceParagraphsPipe } from './slice-paragraphs.pipe';

describe('SliceParagraphsPipe', () => {
  it('create an instance', () => {
    const pipe = new SliceParagraphsPipe();
    expect(pipe).toBeTruthy();
  });
});
